# Brain Drift — 생각의 궤적 레이어 (빌드 계획서)

> **한 줄 정의.** AI ROASTING 블로그의 위키 브레인 위에 얹는 **형제 페이지**. 하나의 개념을 두고, 시간이 흐르며 **내 입장이 어떻게 깊어지고·갈라지고·뒤집혔는지**를 보여준다. 위키가 "지식의 지도"라면, Brain Drift는 "생각의 궤적"이다.
>
> **핵심 구분 (이걸 놓치면 망함).** "개념이 어떻게 변했나"(❌ 남의 뉴스 타임라인)가 아니라 **"그 개념을 보는 *나의 입장*이 어떻게 움직였나"**(✅ 나만의 사고 궤적)다. 가상으로 못 만들고, 위키도 안 하며, 6개월간 실제로 생각이 움직인 사람만 만들 수 있는 콘텐츠.
>
> **카파시 프레임에서의 위치.** 위키 = wiki 레이어(ingest/query, 이미 완성). Brain Drift = **lint 레이어**(모순·진화 감지) — 카파시가 강조했지만 현재 블로그 시스템에 비어 있는 바로 그 조각. `build-report.json`의 `needsReview: 0`이 그 공백의 증거.

---

## 0. 왜 이걸 만드는가 (세 질문에 대한 최종 답)

이 페이지는 세 개의 누적된 질문에 동시에 답한다:

1. **"재미없다 / 생생하지 않다"** → 가상 게스로는 불가능. 실제로 6개월간 움직인 본인 사고만이 생생함의 원천. "내가 어떻게 틀렸고 바뀌었나"는 감정이 실린다.
2. **"블로그와 뭐가 다른가"** → 블로그는 완성된 글을 *읽는* 곳(위→아래). Brain Drift는 같은 글들을 *AI가 어떻게 엮고·모순되고·진화시켰는지 탐험*하는 곳. 같은 재료, 정반대 방향.
3. **"위키에 녹이나 별도인가"** → **별도 페이지, 위키의 형제.** 위키=사전(공간적·정적), Drift=거울(시간적·서사적). 묻는 질문이 다르므로 각자 전체 화면을 받는다. 단 양방향 링크로 연결.

---

## 1. 데이터 기반 (이미 있는 것 / 새로 만들 것)

### 1.1 이미 존재하는 입력 (재사용)
`insights/wiki/concepts-data.json`에 핵심이 다 있다:
- 개념 106개, 각 개념에 **`posts`(언급된 글 목록), `firstSeen`, `lastSeen`, `frequency`, `category`** 보유.
- 즉 "개념별로 어떤 글에 걸쳐 등장했나"의 **시간축이 이미 구축됨.** 별도 추출 거의 불필요.
- 관계 286개(`relationships`: source/target/strength) — 개념 간 인접성.

### 1.2 새로 만들 것 — "입장(stance) 추출"
기존 파이프라인이 하는 건 "이 글에 이 개념이 *나온다*"까지(존재 감지). Brain Drift가 추가하는 건 "이 글이 그 개념을 *어떻게 본다*"(입장 감지).

- 각 (개념 × 글) 쌍에 대해 **입장 요약 1~2문장**을 LLM이 추출.
  - 예: (지식부패 × brain-fry, 3월) → "문제를 *개인 설계자의 인지 부하*로 본다."
  - 예: (지식부패 × ai-slop, 6월) → "문제를 *조직 흐름의 부패*로 본다. 과거 개인 중심 프레임을 명시적으로 반성."
- 이 입장 요약들을 **시간순으로 정렬**하면 한 개념의 궤적이 된다.

---

## 2. 세 가지 카드 유형 (산출물의 핵심 단위)

Brain Drift의 화면은 "카드"의 모음이다. 각 카드 = 한 개념의 한 가지 움직임.

| 카드 유형 | 트리거 신호 | 보여주는 것 | 실제 발굴 예시 |
| --- | --- | --- | --- |
| **🌱 깊어짐 (Deepening)** | 같은 개념, 다른 시점, 입장이 정교해짐 | "A에 대한 시선이 X(과거)→Y(현재)로 이동" | 지식부패: 개인 번아웃(3월) → 조직 흐름 부패(6월) |
| **⚡ 갈라짐 (Tension)** | 같은 개념/주제, 상반된 함의 | "한 글은 기회로, 한 글은 위험으로 본다" | 마찰제거: 해방(next-era) vs 부패(ai-slop), 같은 6월 |
| **🔄 뒤집힘 (Self-revision)** | 글이 과거 프레임을 명시적으로 비판 | "당신이 '지난 2년 시야가 좁았다'고 자기반성" | ai-slop의 "개인 생산성 집중은 시야가 좁았다" |

> **갈라짐(Tension)이 발표의 킬러다.** 본인도 의식 못 한 사고의 긴장을 AI가 짚어주는 순간. "마찰을 없애라 vs 마찰(검증)을 없애면 썩는다" — 청중은 여기서 "이 사람 진짜 깊게 생각하는구나"를 느낀다.

---

## 3. 반자동 워크플로우 (정확성 × 효율의 균형)

완전자동(③)은 입장 감지가 미묘해 헛짚은 카드가 신뢰를 깬다(카파시 경고: 환각이 사실로 굳음). 수작업(①)은 손이 많이 간다. → **반자동(②): 파이프라인이 후보를 뽑고, 사람이 진짜만 확정.**

```
[1단계] 후보 탐지 (자동)
  concepts-data.json에서 다음 조건의 개념을 후보로 추림:
   - posts가 2개 이상 + firstSeen~lastSeen 간격이 충분 (시간차 있음)
   - 우선순위: frequency 높고 카테고리가 research/생존/리더 (입장이 담기는 글)
  → 후보 개념 15~25개 출력

[2단계] 입장 추출 (자동, LLM)
  각 후보 개념의 글들에 대해 입장 요약 1~2문장 생성
  + 세 카드 유형 중 어디에 해당할지 1차 분류 + 신뢰도 점수

[3단계] 큐레이션 (수동, Jayden)  ★여기가 핵심
  파이프라인 제안을 보고:
   - 진짜인 카드만 채택 (헛짚은 것 버림)
   - 입장 요약 문장 다듬기 (본인 목소리로)
   - 카드 유형 확정
  → 최종 카드 5~8개 (질이 양보다 중요)

[4단계] 렌더 (자동)
  확정 카드 → drift-data.json → 페이지 렌더
```

> **정직성 원칙.** 발표·페이지에 "AI가 후보를 찾고, 제가 큐레이션했습니다"를 명시. 이게 오히려 신뢰를 준다 — 카파시의 human-in-the-loop 그대로.

---

## 4. 페이지 설계 (UI 명세)

### 4.1 메뉴 위치
- 위키 브레인 메뉴 **옆에 형제 메뉴 신설**: `Brain Drift` (또는 "생각의 궤적").
- URL: `insights/drift/index.html` (위키와 동일 레벨).

### 4.2 레이아웃
```
┌──────────────────────────────────────────────────┐
│ [HERO] Brain Drift — 생각의 궤적                   │
│  "106개 개념 중, 6개월간 내 입장이 움직인 N개를     │
│   AI가 찾고 내가 골랐습니다."                       │
│  + "AI가 후보 탐지 · 사람이 큐레이션" 배지(정직성)  │
├──────────────────────────────────────────────────┤
│ [필터] 🌱깊어짐  ⚡갈라짐  🔄뒤집힘  (유형별)        │
├──────────────────────────────────────────────────┤
│  [카드 1] ⚡ 갈라짐 — "마찰 제거"                   │
│   ┌────────────────┬────────────────┐            │
│   │ next-era (6월)  │ ai-slop (6월)   │            │
│   │ "마찰 없애면     │ "검증 마찰      │            │
│   │  중간층 해방"    │  없애면 부패"    │            │
│   └────────────────┴────────────────┘            │
│   → 한 줄 통찰: "어떤 마찰을 없애고 지킬 것인가"     │
│   [원문 글 보기] [관련 개념: 업무재설계 →위키]       │
├──────────────────────────────────────────────────┤
│  [카드 2] 🌱 깊어짐 — "지식 부패"                   │
│   3월 ●━━━━━━━━━━● 6월  (미니 타임라인)            │
│   개인 번아웃  →  조직 흐름 부패                     │
│   "당신은 문제의 위치를 사람에서 시스템으로 옮겼다"  │
├──────────────────────────────────────────────────┤
│  ... 카드 5~8개 ...                                │
└──────────────────────────────────────────────────┘
```

### 4.3 카드 상세 (유형별 시각)
- **🌱 깊어짐:** 좌→우 미니 타임라인. 과거 입장 → 현재 입장. 화살표 위에 "무엇이 깊어졌나" 한 줄.
- **⚡ 갈라짐:** 좌우 분할 대비. 두 글의 상반된 입장을 나란히. 하단에 "이 긴장이 묻는 질문" 한 줄.
- **🔄 뒤집힘:** 인용 블록으로 자기반성 문장을 크게. "과거 프레임 → 반성 → 새 프레임" 3단.
- 모든 카드 공통: 원문 글 링크 + 관련 위키 개념 링크(형제 페이지로 다리).

### 4.4 위키와의 양방향 다리 (녹이지 않되 잇는다)
- **위키 개념 페이지 하단** → "이 개념에 대한 생각의 변화 보기 →" 링크 (해당 Drift 카드로).
- **Drift 카드** → "관련 개념" 클릭 시 위키 개념 페이지로.
- 두 페이지는 독립이되, 사전↔거울을 자유롭게 오간다.

---

## 5. 디자인 (블로그 톤 계승)
- 블로그 기존 CSS·다크모드·헤더 그대로 계승 (`assets/css/wiki.css` 참고, drift 전용 css 추가).
- `HEADER_CONFIG.activeCat = 'drift'`로 헤더 활성 메뉴 처리(위키와 동일 패턴).
- 카드 유형별 색: 🌱깊어짐=초록, ⚡갈라짐=주황(테라코타), 🔄뒤집힘=보라. 한눈에 움직임의 종류가 읽히게.
- **시그니처:** ⚡갈라짐 카드의 좌우 분할 대비. 여기 한 곳에 시각적 투자 집중.

---

## 6. 빌드 단계 (Claude Code 작업 순서)

**Phase 1 — 후보 탐지 스크립트**
1. `_internal/scripts/brain-drift/detect-candidates.js` — concepts-data.json 읽어 시간차 있는 개념 후보 15~25개 출력(3단계 조건).
2. 기존 wiki-brain 파이프라인 구조·코드 스타일 그대로 따름(형제 스크립트).

**Phase 2 — 입장 추출**
3. `extract-stance.js` — 후보 개념의 각 글에서 입장 요약 + 카드유형 1차분류 + 신뢰도. (LLM 호출. 빌드타임 1회, 결과 캐시)
4. 출력: `drift-candidates.json` (큐레이션 대기 목록).

**Phase 3 — 큐레이션 (수동)**
5. Jayden이 candidates 검토 → 채택/수정/유형확정 → `drift-data.json` 확정. (간단한 검토용 로컬 뷰 있으면 편함. 선택)

**Phase 4 — 렌더**
6. `render-drift.js` — drift-data.json → `insights/drift/index.html`. 위키 render 스크립트 패턴 재사용.
7. 카드 3유형 컴포넌트 구현. ⚡갈라짐 좌우분할에 공들이기.

**Phase 5 — 연결 & 마감**
8. 위키 개념 페이지에 "생각의 변화 보기" 링크 주입(inject 스크립트 확장).
9. 헤더 메뉴에 Brain Drift 추가. 반응형·다크모드 확인.
10. `.claude/skills/update-wiki/` 옆에 `update-drift` 스킬 추가 검토(새 글 들어오면 후보 갱신).

---

## 7. 범위 가드레일

**꼭 한다:**
- 카드 **5~8개, 질 우선.** 헛짚은 카드 0개가 카드 20개보다 낫다.
- 세 유형 각각 최소 1개씩 (특히 ⚡갈라짐 — 킬러).
- "AI 탐지 + 사람 큐레이션" 정직성 배지.
- 위키 양방향 링크.

**안 한다 (이번엔):**
- 완전 자동 감지(③). 신뢰도 관리 안 되면 역효과.
- 106개 전체 커버. 움직임이 또렷한 개념만.
- 실시간 재계산. 빌드타임 1회로 충분.

**나중에 확장:**
- 새 글 추가 시 후보 자동 재탐지(update-drift 스킬).
- "이 개념, 다음엔 어디로 갈까" 예측 카드(실험적).
- 영문 카드(해외 청중·CAIO급 대상).

---

## 8. 발표 동선 (포럼/실무 시연용)

위키→Drift 순서가 서사를 만든다:
1. **위키 그래프 먼저.** "이게 제 6개월 지식의 *지도*입니다. 106개 개념, 286개 연결."
2. **Drift로 전환.** "그런데 지도는 *지금*의 모습이죠. 제 생각이 *어떻게 거기 도달했는지*는 안 보입니다."
3. **⚡갈라짐 카드.** "AI가 제 글에서 이런 긴장을 찾아냈어요 — 같은 달에 마찰을 해방으로도, 부패로도 봤더라고요. 저도 몰랐습니다."
4. **🔄뒤집힘 카드.** "6월의 저는 3월의 제 프레임이 좁았다고 스스로 반성했더군요. AI가 그걸 짚어줬습니다."
5. **마무리.** "이게 카파시가 말한 lint입니다 — 지식이 쌓이기만 하는 게 아니라, 스스로를 돌아봅니다. 그리고 이건 제 *실제* 생각이라, 가상 데모로는 만들 수 없습니다."

> 핵심 메시지: **"AI는 내 답을 대신 쓰지 않았다. 내 생각이 어떻게 움직였는지 비춰줬다."**

---

## 9. 자기평가 체크리스트 (출격 전)
- [ ] 카드가 "개념 변화"가 아니라 "내 *입장* 변화"를 보여주는가 (핵심 구분)
- [ ] ⚡갈라짐 카드가 최소 1개, 진짜로 통찰을 주는가
- [ ] 헛짚은 카드 0개인가 (큐레이션 통과)
- [ ] 위키와 양방향 링크가 작동하는가
- [ ] "AI 탐지+사람 큐레이션" 정직성이 명시됐는가
- [ ] 발표 동선(8장)대로 실제로 말해봤는가
